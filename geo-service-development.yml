
server:
  port: 5000
  servlet:
    context-path: /geo-api 

base-url: http://localhost:${server.port}/${server.servlet.context-path}      
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
conn_string: postgresql://postgres:postgres@localhost:5432/geospatial
instance_host: localhost
app_name: geo-service
ZIPKIN_DSN: http://127.0.0.1:9411/api/v1/spans
ZIPKIN_DISABLE: False
